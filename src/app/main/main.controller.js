(function() {
  'use strict';

  angular
    .module('AllophonicTranscriptor')
    .controller('MainController', MainController);

  /** @ngInject */
  function MainController($scope) {
    // exercise.getGaps(function(data) {
    //   $scope.gaps = data;
    // });

    // $scope.evaluate = function() {
    //   $scope.score = itemProvider.evaluateAnswers();
    // };

  }
})();
